{
    "makefile.launchConfigurations": [
        {
            "cwd": "${workspaceFolder}",
            "binaryPath": "${workspaceFolder}/executable.out",
            "binaryArgs": []
        }
    ],
    "makefile.buildLog": ""
}
